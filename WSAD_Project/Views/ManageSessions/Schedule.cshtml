@model List<WSAD_Project.Models.ViewModels.ManageSession.SessionScheduleViewModel>

@{
    ViewBag.Title = "Registered";
}

<h2>Session Schedule</h2>

@using (Html.BeginForm("RemoveSession", "ManageSessions"))
{
    <button type="submit" class="btn btn-danger">Remove Selected Sessions</button>
    @Html.ActionLink("Cancel Changes", "Index")

    <table class="table">
        <tr>
            <th>
                &nbsp;
            </th>
            <th>
                Title
            </th>
            <th>
                Description
            </th>
            <th>
                Address
            </th>
            <th>
                Room
            </th>
            <th>
                Time
            </th>
            <th>
                Occupancy
            </th>
            <th></th>
        </tr>

        @for (int i = 0; i < Model.Count; i++)
        {
            var item = Model[i];
            <tr>
                <td>
                    @Html.HiddenFor(x => Model[i].Id)
                    @Html.CheckBoxFor(x => Model[i].IsSelected)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Title)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Address)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Room)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Time)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Occupancy)
                </td>
                <td>
                    @*                    @Html.ActionLink("Details", "Details", "ManageSessions", new { id = item.Id }, null)
                    *@
                </td>
            </tr>
        }

    </table>

    <button type="submit" class="btn btn-danger">Remove Selected Sessions</button>
    @Html.ActionLink("Cancel Changes", "Index")

}
